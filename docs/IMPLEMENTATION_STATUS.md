# حالة التنفيذ مقابل Master Spec 1.1.3

المواصفات محفوظة دون تعديل في `spec-v1.1.3/`. لا توجد قرارات معمارية جديدة أو رفع لإصدار المواصفات. هذه الدفعة تتبع البداية الإلزامية في `DEVELOPMENT_PLAN.md`.

| المرحلة | الحالة | الدليل / ما تبقى |
|---|---|---|
| بوابة عقد 1.1.3 | اجتازت | فحص البصمات و49 فحصًا في harness الأصلي؛ توليد أنواع TypeScript بعده |
| T-00 البيئة | جزئية | CI ناجح: Node 24 وWordPress 7.1/PHP 8.4 ومصفوفة MySQL 8.4 وMariaDB 10.11؛ إعدادات خادم xCloud غير مفحوصة |
| T-01 PoC renderer | كود منفذ، والقبول البصري معلق | DOM عربي + أشكال SVG + إحداثيات normalized؛ اختبارات التحويل والمتصفحات على GitHub تنجح؛ اعتماد الأجهزة واللقطات البصرية باقٍ |
| T-02 PoC editor input | منفذ جزئيًا؛ البوابة مفتوحة | React/Moveable، textarea، transform controls، zoom/pan؛ اختبار الأجهزة الفعلية وبقية سيناريوهات الحفظ/القفل غير منفذ |
| T-03 | منفذ؛ اجتازت فحوص التكامل | Composer PSR-4، التفعيل والترقية، إصدارات schema/roles وحفظ البيانات عند التعطيل |
| T-04 | جزئية | الجداول التسعة، transaction، ChapterRepository/DTO، Geometry validator؛ بقية repositories/validators/services لم تكتمل |
| T-05 | منفذ؛ اجتازت فحوص التكامل | CPT، التصنيفات والأنواع الستة، registered metadata وصلاحيات Core REST |
| T-06 | منفذة؛ اجتازت أساسياتها CI #13 | CRUD الفصول، مراجعة مستقلة، رفع طلبين متزامنين بمفتاح ثابت، فحص الصور، ترتيب two-phase وحذف البيانات المرتبطة؛ اختبارات سباق حذف العمل/إنشاء الفصل |
| T-07 | منفذة للموارد العامة الحالية؛ اجتازت CI #13 | library/work/chapter/pages/overlays/contributors/profiles/capabilities؛ عقد DTO والسياسة العامة للمسودة والعمل غير المنشور |
| T-08 | منفذة؛ اجتازت CI #13 | قالب WordPress عربي SSR للمكتبة والفلاتر والعمل والفصول والملف العام؛ URL filters وحالات المحتوى الفارغ |
| T-09 | منفذة جزئيًا؛ اجتاز أساسها CI #13 | قارئ webtoon/paged، RTL/LTR، عارض مشترك، toggle دون استبدال الصور، تقدم زائر محلي وعضو عبر PUT؛ قبول pinch/pan الفعلي وبلاغات T-17 باقيان |
| T-10 | هيكل متصل منفذ؛ نتيجة الرأس في PR #1 | مسار محمي داخل الإضافة، REST للبيانات، state/navigation بمعرّفات الصفحات والعناصر، stage بأبعاد الصور الفعلية، properties/layers للعرض، Preview؛ لا كتابة بعد |
| T-11 | منفذة ومتّصلة بالحفظ | الأنواع الأربعة وخصائصها وMoveable والبدائل الرقمية والنسخ والحذف والترتيب |
| T-12 | منفذة؛ نتيجة الرأس في PR #1 | POST/PATCH/DELETE، تحقق صارم للنمط، autosave وحالات الشبكة والتعارض |
| T-13 | منفذة؛ اختبار proxy الفعلي باقٍ | قفل ذري 45s وتجديد 15s، If-Match/ETag، force-release بنفس المسار، و412/423/428/409 |
| T-14 | منفذة | مساهمة فريدة لكل عنصر ومستخدم داخل transaction؛ الحذف يزيل إسناد العنصر |
| T-15–T-16 | جزئية | حل أولوية الأنماط عند الإنشاء؛ إدارة presets وsnapping وتحسينات الجوال والقبول الفعلي باقية |
| T-17–T-20 | لم تبدأ | المجتمع والبلاغات والتحصين والأداء والقبول والنشر |

## متطلبات تدعمها التجربة دون ادعاء قبول الميزة كاملة

- FR-ED-002/003/004/005/006/007/008/009/010/012: نماذج الأنواع والتحويلات والعرض، ضمن الجلسة فقط.
- NFR-RTL-001 وNFR-A11Y-001: RTL وحقول وأزرار بديلة للسحب، مع مراجعة قابلية الاستخدام على الأجهزة باقية.
- AC-02: فقط ثبات الإحداثيات والمعاينة وإخفاء الترجمة في هذه الدفعة؛ أضيف قارئ عام وسياسة مسودات؛ القبول على الأجهزة الفعلية لا يزال معلقًا.
- AC-03: أمان النص في العارض واختبارات المتصفح؛ لا يشمل validator الخادم أو اعتماد الخط على الأجهزة.
- AC-04/05/12: أضيف الحفظ والأقفال والتعارض والمساهمات؛ يبقى القبول الكامل والأجهزة الفعلية.
- AC-CONTRACT-1: يُفحص عقد المصدر واستجابات REST الفعلية؛ عقود presets والبلاغات غير منفذة بعد.

## اختيارات التنفيذ المحلية

- أطوال النمط المرئية (font/border/padding) تحوّل بالنسبة لعرض الصورة، بينما Y/H بالنسبة لارتفاعها. جميع الأبعاد تقاس من الصورة الفعلية دون حشو مساحة العمل.
- `sfx-display-1` مربوط مؤقتًا بخط Tajawal 900 في تجربة الأجهزة، لأن المواصفات لم تعين ملف الخط الزخرفي النهائي. لم يُعتمد نمط SFX إنتاجي.
- الصورة المرجعية رسم هندسي أصلي لاختبار التغطية والمواضع. لا توجد أعمال أو فصول أو مساهمات وهمية في WordPress.
- `OverlayDraft` مشتق من أنواع العقد لكنه يحمل مفتاح جلسة محليًا؛ لا IDs أو versions أو نسب مساهمات محفوظة مخترعة.
- أثناء الإيماءة تبقى البكسلات محلية؛ يرسل المحرر الهندسة عند نهاية التحويل. يظهر “تم الحفظ” بعد تأكيد الخادم فقط. تجربة PoC مستقلة وغير متصلة.
- السحب على العنصر يحوّله، واللمس على المساحة الفارغة يحرك الصفحة؛ إصبعان على المساحة الفارغة يكبران السطح. لا يستخدم pinch كبديل وحيد للدوران.

## بوابة الأجهزة — لا تُغلق بالمحاكاة

اختبر على iPhone/iPad حقيقي وAndroid حقيقي وسجل الطراز وإصدار النظام والمتصفح والنتيجة:

| السيناريو | iOS فعلي | Android فعلي |
|---|---|---|
| drag / resize / rotate قبل التكبير وبعده | معلق | معلق |
| pinch zoom وpan دون تعارض مع تحريك العنصر | معلق | معلق |
| textarea عربي، فتح/إغلاق لوحة المفاتيح واللوحة السفلية | معلق | معلق |
| النمط الأساسي، الأنواع الأربعة، معاينة النتيجة | معلق | معلق |
| وصلات الحروف العربية مع SFX stroke | معلق | معلق |
| تطبيق preset محفوظ، autosave، lock conflict | يتطلب T-12/T-13/T-15 | يتطلب T-12/T-13/T-15 |

مواصفات الجوال تطلب التحقق قبل بناء المحرر كاملًا. لا توجد نتيجة أجهزة مخمنة، ولا يُستبدل Moveable أو نموذج البيانات بناءً على المحاكاة وحدها.

## GitHub والنشر

حدد المستخدم المستودع `barod1986-ship-it/Manga-Overlay`، وتأكدت صلاحية الكتابة. كان المستودع فارغًا؛ تمت تهيئة `main` بملف تعريف، وتُرفع دفعة البداية على `codex/initial-renderer-editor` للمراجعة. نتائج التشغيل على GitHub موثقة في `verification/RESULTS.md` وسجل Actions.

لم يتم الدخول إلى xCloud أو الخادم. ملف GitHub Actions يشغل فحوص الواجهة، وأضيفت مصفوفة MySQL 8.4 وMariaDB 10.11 عبر WordPress 7.1/PHP 8.4 و`dbDelta()` الفعلية. نجح 71 فحصًا على كل محرك في تشغيل #6؛ لا توجد اختبارات نشر على NGINX الفعلي.

## استئناف 2026-09-06 — Core 0.4.0 / Theme 0.1.0

المواصفة المرفقة في الاستئناف طابقت الملفات الأربعين في المستودع byte-for-byte. بدأ الاستئناف من `440b290`، وليس من تاريخ T-13/T-15 في نسخة مستودع سابقة.

- آخر CI سابق توقف لأن مساعد الاختبار فرض array على DELETE 204 الفارغ. الإصلاح `76893e1` أوصل فحوص المحتوى إلى 162 ناجحة ثم كشف استخدام URL REST ثابت في اختبار HTTP؛ أصبح الاختبار يستخدم bootstrap WordPress الفعلي.
- WorkMutationLock ينسق إنشاء الفصل وحذف/نقل العمل للسلة عبر GET_LOCK على الاتصال، مع إعادة قراءة الأب بعد انتظار القفل وإطلاقه بعد اكتمال Core/shutdown. لا تعديل للجداول أو عقد المواصفة.
- قراءة القارئ العامة لا ترث صلاحيات المدير، ولا تظهر فصلًا منشورًا تحت عمل غير منشور.
- `/reading-progress` يستخدم عقده الأصلي؛ UPSERT منفصل لكل عضو/فصل، مع رفض موضع خارج الصفحات وموارد غير عامة. لا endpoint جديد لاستعادة التقدم؛ يضمَّن تقدم العضو الحالي فقط في HTML خاص غير قابل للتخزين المشترك.
- القارئ يضمّن overlay batch مرة واحدة، ولا ينشئ طلبًا لكل عنصر. الصور بأبعادها الصريحة والأولى eager؛ toggle يبقي عقد الصور، ويمسح ظهور الطبقة بأكملها.
- التكبير بأزرار حتى 300% مع overflow-pan، وpinch المتصفح متاح دون تعطيل تمرير اللمس. التحقق الفعلي من تجربة pinch داخل viewer على iOS/Android باقٍ.
- شاشة المحرر الإنتاجية والحفظ والأقفال على العناصر والـpresets والبلاغات لا تزال مهام لاحقة؛ لا تظهر أزرار موصولة بوظائف غير منفذة في القارئ.

ارتباط المتطلبات: FR-LIB-001/002/003/004، FR-WORK-001/002، متطلبات القارئ في READER_SPEC، FR-CMS-001 وFR-ADM-002؛ تحقق جزئي لـAC-01/02/03/10/11. لا ادعاء بإغلاق T-19 أو T-20.

اختبار الخادم الحقيقي والنشر إلى xCloud مؤجلان بطلب المستخدم. تبقى أجهزة الجوال الحقيقية وPHPCS/PHPStan وESLint/Prettier والأداء وسياسات cache النهائية بوابات مفتوحة. نتائج التشغيل المثبتة في `verification/RESULTS.md`؛ رأس المتابعة النهائي ونتيجته موثقان في PR #1. أضيف ترتيب رقمي للصفحات وإثبات UI لحفظه، واختبار UI لاستعادة تقدم العضو بعد reload.

## T-10 — Core 0.5.0 / Theme 0.1.1

- المسار الأصلي `/series/{slug}/chapter/{chapter}/edit/` يتحقق من جلسة WordPress و`mol_use_editor` قبل البحث عن المورد. الزائر يُحوَّل لتسجيل الدخول، والعضو دون الصلاحية يحصل على 403؛ المورد المفقود بعد التحقق يعطي 404.
- قالب المحرر وحزمته داخل الإضافة. HTML يحمل سياقًا صغيرًا وnonce للحساب الحالي دون payload الصفحات/العناصر؛ يقرأ React الفصل وصفحاته وعناصر الصفحة عبر عقود GET الأصلية. قراءة المسودات تبقى خلف سياسة REST، ولا تتغير دوال القراءة العامة.
- `#page=<id>&element=<id>` حالة تنقل في المتصفح، وليست route API جديدة. Back/Forward/reload يستعيد التحديد. تُلغى القراءة القديمة عند تغيير الصفحة، وتختفي طبقاتها فورًا أثناء التحميل.
- Stage يستخدم عرض/ارتفاع الصورة الحقيقيين والعارض DOM/SVG المشترك، مع zoom وoverflow-pan، وPreview يخفي التحديد واللوحات دون استبدال عقدة الصورة.
- قائمة الطبقات مرتبة للعرض فقط؛ IDs وversions والإسناد محفوظة في DTO الأصلية. لوحة الخصائص للقراءة، ولا يظهر مؤشر حفظ زائف أو أدوات تعديل غير موصولة. لا تُكتب بيانات الفصل في localStorage.
- أضيفت روابط من الإدارة والقارئ لمن لديه الصلاحية، ومعالجة empty/loading/404/network/expired-session. بيانات الاختبار تشمل صفحة طولية وأخرى عريضة ومسودة بأربعة أنواع عناصر.

يغطي ذلك FR-ED-001/010/012 وNFR-RTL-001، وجزءًا من FR-CH-002 وFR-ADM-003 وAC-02. T-11/T-12/T-13 وقياسات الأجهزة والقبول البصري ما زالت لاحقة؛ هذه الدفعة لا تغلق بوابة MOBILE_SPEC أو تعتمد المحرر كاملًا. نتيجة CI على الرأس النهائي وروابط الحزم في [PR #1](https://github.com/barod1986-ship-it/Manga-Overlay/pull/1).


## T-11 — Core 0.6.0 / Theme 0.1.1

تحرير مؤقت متصل بصفحات WordPress الحقيقية. لكل صفحة نسخة عمل في ذاكرة React تحفظ المصدر وversion منفصلين عن تعديلات النص والنمط والهندسة. العناصر الجديدة والنسخ تحمل مفتاح جلسة فقط، بلا id/version/نسب مساهمة مخترعة. يبقى المصدر دون تعديل. لا طلبات كتابة ولا localStorage/IndexedDB لمسودات المحرر، ويظهر تنبيه قبل مغادرة جلسة تغيرت.

- إضافة الأنواع الأربعة، تحديدها، تحرير النص الآمن وخصائص الخط والخلفية والحدود والزوايا والحشو والظل. خيارات tail خاصة بالفقاعة، وخيارات burst/scale خاصة بـSFX.
- تحريك/تغيير حجم/دوران عبر Moveable؛ البكسلات محلية أثناء الحركة ثم تتحول إلى قيم normalized مقيدة بحدود العقد عند نهايتها. يلغي تغيير الصفحة أو إخفاء الطبقات أو تغيير أبعاد العرض التحويل المعلق.
- بدائل رقمية وأزرار خطوة لجميع التحويلات، وترتيب z-index، وDelete وCtrl/Cmd+D والأسهم وEsc خارج الحقول ومع استثناء تركيب النص IME.
- النسخ والحذف والتراجع عن آخر حذف محلي لكل صفحة. التعديلات تبقى عند التنقل بين صفحات الفصل وفي المعاينة؛ إعادة التحميل بعد قبول تنبيه المغادرة تعيد بيانات REST.
- دخول المحرر يعتمد mol_use_editor؛ التعديل يعتمد mol_edit_translations، والحذف يتطلب كذلك mol_delete_translation_elements. فشل الجلسة يغلق سطح التحرير ويمسح نسخه المؤقتة. هذه ضوابط واجهة T-11 وليست بديلًا عن تحقق الخادم في T-12/T-13.

تغطي هذه الدفعة FR-ED-002/003/004/005/006/007/010/012 وبدائل السحب من NFR-A11Y-001، ضمن الجلسة فقط. تدقيق العربية/SFX البصري والأجهزة الحقيقية ما زال مفتوحًا. الخادم يعيد الأنماط القائمة كما هي؛ حل presets الافتراضية وsnapping وauto-fit الكامل في T-15. الحركة بإصبعين وkeyboard على الأجهزة الفعلية في T-16. لا تدعي هذه الدفعة اكتمال AC-04/05/12 أو الحفظ والتعاون.

فحوص هذه الدفعة تعمل في CI فقط؛ أحدث رأس ونتائجه والحزمة موثقة في PR #1. اختبار xCloud والنشر ما زالا مؤجلين.

## T-12/T-13/T-14 — Core 0.7.0

الحفظ الخادمي يستخدم المعاملات والجداول الأصلية دون تغيير schema 1.1.3. يطبق الخادم مخطط ElementCreate/ElementPatch وقيود النوع الثابت والهندسة؛ 2924 حالة مشتقة من المخطط تُقارن بمدقق JSON Schema مستقل في CI. الأنماط الأساسية مشتركة بين PHP وTypeScript، ويحل إنشاء العنصر أولوية preset الصريح ثم الشخصي والعمل والعام ثم الأساس، مع override أخير. إدارة presets لا تزال T-15.

كل عنصر له طابور حفظ مستقل ومفتاح POST ثابت مع payload ثابت عند غموض نتيجة الشبكة. الكتابة أثناء طلب جارٍ لا تُفقد. PATCH يبني If-Match من النسخة المؤكدة فقط. عند انقطاع PATCH يعاد فحص النسخة قبل إعادة المحاولة؛ لا overwrite صامت. المقارنة تعرض النص والهندسة والنمط وتتيح الحالية أو إعادة تطبيق الحقول المعدلة على الحالية. الأقفال لا تكشف token لمستخدم آخر، وتنتهي بعد 45 ثانية؛ تجدد الواجهة كل 15 ثانية أثناء التحديد/التعديل.

الحذف الخادمي يحتاج تأكيدًا للعنصر المحفوظ، مع تراجع قصير قبل إرسال الطلب. تزيل المعاملة العنصر والقفل والمساهمات وتفرغ مرجع البلاغ دون حذف البلاغ نفسه. الحفظ يحدّث مساهمة واحدة لكل عنصر ومستخدم؛ لا عداد لعدد الحفظات. الفشل في المساهمة يعيد المعاملة كاملة.

يغطي ذلك FR-SAVE-001/002/003 وFR-LOCK-001/002 وFR-CONC-001/002 وFR-CON-001/002، مع تفاصيل الفحوص في verification/RESULTS.md. فحوص WordPress والمتصفحات في CI، واختبار خادم xCloud ومرور الهيدرات عبر NGINX والأجهزة الفعلية مؤجل بطلب المستخدم. الأقسام T-10/T-11 أعلاه توثق تاريخ الدفعات، وقيودها السابقة على الكتابة تجاوزتها هذه الدفعة.
