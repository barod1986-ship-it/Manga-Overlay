# Documentation

`master-spec/v1.1.3/` is the immutable source package supplied for MVP implementation. Its internal `SHA256SUMS.txt` verifies the copied specification files.

Implementation notes live outside that directory so the frozen source can always be compared with its published checksums.
