{
  "name": "manga-overlay",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "poc/renderer"
  ],
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build:poc": "npm --workspace @mol/poc-renderer run build",
    "check:poc": "npm --workspace @mol/poc-renderer run check",
    "dev:poc": "npm --workspace @mol/poc-renderer run dev",
    "test:poc": "npm --workspace @mol/poc-renderer run test"
  }
}
