# Manga Overlay query REST requests must reach PHP for every HTTP method.
# Install inside the existing main location. Original query arguments are retained.
if ($arg_rest_route ~* "^(/|%2f)mol(/|%2f)v1(/|%2f|$)") {
    rewrite ^ /index.php last;
}
