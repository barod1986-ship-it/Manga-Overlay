# Manga Overlay — إدارة المحتوى والمكتبة والقارئ العربي

تأسيس إضافة WordPress وتطوير **تجربتي العارض وإدخال المحرر T-01 / T-02** وفق Master Spec `1.1.3`. يشمل التفعيل والجداول والأدوار والأعمال، مع بقاء بقية المنصة قيد التنفيذ. اسم MOL تقني مؤقت كما في المواصفات.

**هذه نسخة تطوير؛ الموقع الكامل لم يكتمل.** Core 0.11.0 يدير الفصول والصفحات ويشغّل المكتبة والقارئ مع القالب المرفق. أضيف تحرير العناصر T-11 داخل المحرر المتصل: أربعة أنواع وخصائص وسحب وتغيير حجم ودوران. أضيف حفظ العناصر إلى الخادم مع autosave وأقفال وإصدارات ومقارنة صريحة عند التعارض؛ تحتسب المساهمات لكل عنصر ومستخدم. يعيد التصحيح 0.7.1 التحقق من الصفحات بقراءة حديثة بعد القفل عند الحذف المتزامن. تجربة إدخال المحرر PoC مستقلة، وتغييراتها محلية داخل الجلسة. المستودع المعتمد هو [barod1986-ship-it/Manga-Overlay](https://github.com/barod1986-ship-it/Manga-Overlay). حُدّث الموقع المستهدف إلى Core 0.11.0 في 2026-09-12 مع بقاء القالب 0.1.2. نجح الرفع والترتيب ومراجعة البلاغات. أُصلح مسار ETag في إعدادات xCloud في 2026-09-10؛ أكد المحرر الحفظ التلقائي وبقي النص العربي بعد إعادة التحميل. تفاصيل الفحوص وحدودها في [سجل التحقق](docs/verification/RESULTS.md).

دفعة Core 0.11.0 تضيف سياسة أمان خاصة بالمحرر: nonce عشوائي لكل استجابة لسكربتات WordPress، ومنع السكربتات المضمنة غير المصرح بها وeval وتضمين المحرر من موقع آخر. اجتازت الدفعة جميع مهام Actions #42 وثُبّتت حزمتها على الموقع. نجح فحص ترويسات المحرر الفعلية والحفظ وإعادة التحميل ومعاينة النص العربي. روابط الحزمة والبصمات في PR #1 وسجل التحقق.

## تفعيل إضافة WordPress

راجع [دليل تطوير WordPress](docs/WORDPRESS_DEVELOPMENT.md) لبناء Composer autoloader أو استخدام ZIP من artifact `manga-overlay-development-package` بعد نجاح Actions، ثم تثبيت الإضافة في بيئة تطوير مطابقة للمواصفات. التفعيل ينشئ الجداول التسعة، ويثبت الأدوار، ويسجل الأعمال وتصنيفاتها. لا يوجد نشر آلي إلى خادم المستخدم.

## تشغيل تجربة المحرر

المتطلب: Node.js 24 LTS. من جذر المشروع:

```bash
npm ci
npm run dev
```

افتح `http://127.0.0.1:5173`. تعرض التجربة صفحة مرجعية أصلية أُنشئت لاختبار مواضع الترجمة، وليست محتوى للمكتبة.

للبناء:

```bash
npm run build
```

ينتج أمر البناء نسخة في `wp-content/plugins/manga-overlay-core/assets/dist/poc/`، وملف `preview.html` لمعاينة محلية. لم يُتحقق من فتح الملف في متصفح هذه الجلسة بسبب قيود بيئة الفحص. المسار المعتمد للتجربة هو HTTP محلي؛ يمكن تشغيله دون تثبيت Node:

```bash
python -m http.server 8080 --bind 127.0.0.1 --directory wp-content/plugins/manga-overlay-core/assets/dist/poc
```

ثم افتح `http://127.0.0.1:8080`. ملفات JavaScript والخطوط محلية؛ افتحها عبر خادم HTTP المحلي لأن فتح HTML مباشرة من نظام الملفات لا يكفي لوحدات JavaScript.

## المنفذ

- تأسيس إضافة `manga-overlay-core` مع Composer PSR-4 وتفعيل وترقيات ذات إصدارات مستقلة للبيانات والصلاحيات.
- الجداول التسعة عبر dbDelta، وفحص InnoDB/utf8mb4، ودعم transaction rollback.
- أدوار وصلاحيات مستقلة، وتسجيل `mol_work` والتصنيفات وحقول العمل عبر WordPress Core REST.
- منع حذف عمل ذي فصول عبر WordPress، وتنسيق إنشاء الفصل مع حذف الأب لمنع السباق.
- إدارة عربية للفصول والرفع والترتيب والمراجعة، وواجهات عامة typed للمكتبة والأعمال والفصول والصفحات والمساهمين.
- قالب عربي للمكتبة والعمل والملف العام، وقارئ webtoon/paged يستخدم العارض المشترك، مع تقدم محلي للزائر ومرتبط بحساب العضو.
- حزمة قارئ مستقلة في `assets/dist/reader/`، وحزمة قالب WordPress منفصلة عن Core.
- هيكل محرر في `/series/{slug}/chapter/{chapter}/edit/` مملوك للإضافة: يتطلب `mol_use_editor`، ويحمل الفصول والصفحات والعناصر من REST الموثّق. يوفر تنقلًا بمعرّفات ثابتة، وطبقات وخصائص للعرض، ومعاينة تستخدم العارض نفسه، وحالات خطأ/إعادة محاولة. يحفظ التعديلات عبر REST الموثّق؛ التغييرات غير المرسلة تبقى في ذاكرة الجلسة فقط.

- حفظ تلقائي بعد 1200ms، وحفظ التحويل عند نهاية الحركة؛ مفتاح ثابت لإنشاء العنصر، وIf-Match/ETag وlease لمدة 45 ثانية مع تجديد كل 15 ثانية.
- التحقق من النمط ونوع العنصر في الخادم، والمساهمات الفريدة، وحذف البيانات التابعة داخل transaction.
- عارض DOM للنص العربي وSVG للأشكال، مستخدم في التحرير والمعاينة.
- أربعة أنواع: bubble / narration / free_text / sfx، مع الأنماط الأساسية المحددة في المواصفات.
- إحداثيات نسبية بوحدة مليون، ودوران milli-degrees، وتحويل مستقل عن عرض النافذة.
- React + TypeScript strict + react-moveable مع lockfile مثبت.
- تحديد، إضافة، نسخ، حذف مع تراجع مؤقت، ترتيب طبقات، تحرير نص، ألوان، نمط خط، تحريك، تحجيم ودوران.
- بدائل رقمية وأزرار نقل دقيق؛ اختصارات لا تعترض الكتابة داخل textarea.
- معاينة وإخفاء الترجمة دون تغيير الصورة الأصلية، وملاءمة النص ضمن حد الحجم الأدنى.
- خصائص الجوال في لوحة سفلية، وتكبير/تحريك سطح الصفحة، وخطوط عربية مستضافة محليًا.
- أنواع API مولدة من العقد بعد اجتياز فحصه؛ لا mock endpoints ولا نسخة مخترعة من عقد البيانات.
- GitHub Actions يشغّل العقد وTypeScript والبناء واختبارات المتصفح ومصفوفة WordPress/قواعد البيانات.

## الاختبارات

```bash
python -m pip install -r requirements-dev.txt
npm run check
npx playwright install chromium firefox webkit
npm run test:e2e
```

إعادة توليد أنواع OpenAPI:

```bash
npm run generate:types
```

يفحص `scripts/check-spec.py` بصمات الملفات ثم يشغّل `VALIDATION_HARNESS.py` الأصلي. لا تعدّل `generated/api.d.ts` يدويًا.

راجع `docs/IMPLEMENTATION_STATUS.md` و`docs/verification/RESULTS.md` لمعرفة الاختبارات المنفذة والبوابات المتبقية. اجتازت اختبارات المتصفح على GitHub بعد إصلاح التحجيم ولوحة خصائص الجوال: 33 ناجحة واختبارا فأرة مستثنيان في محاكاة الهاتف. محاكاة الهاتف في Playwright لا تحل محل اختبار iPhone وAndroid فعليين الذي تفرضه المواصفات.

## هيكل التنفيذ

```text
docs/spec-v1.1.3/                 المواصفات الأصلية كاملة دون تعديل
docs/verification/                نتائج الفحوص والقيود المسجلة
wp-content/plugins/manga-overlay-core/
  src/                            التفعيل والصلاحيات والخدمات وREST ومسارات القارئ
  database/schema.sql             SQL المطابق للمواصفات
  composer.json                   autoload لـPHP
  editor-src/domain/              الأنماط والتحويلات والأنواع المشتقة
  editor-src/renderer/            العارض المشترك
  editor-src/poc/                 تجربة المحرر؛ الحالة محلية فقط
  editor-src/reader/              القارئ العام وحفظ تقدم القراءة
  editor-src/editor/              المحرر المتصل والحفظ والأقفال ومقارنة التعارض
  templates/editor.php            قالب المحرر داخل الإضافة
  editor-src/generated/           أنواع OpenAPI المولدة
  public/reference-page.svg       صورة اختبار أصلية
wp-content/themes/manga-overlay-theme/  القالب العربي للمكتبة والعمل والقارئ
tests/                            اختبارات المجال والمتصفحات وتكامل WordPress
```

مجلد `poc` غلاف تجربة؛ العارض والتحويلات مشتركان بالفعل بين تجربة المحرر والقارئ العام. المسار الحالي داخل `wp-content` هيكل مستودع كما تصفه المواصفات، ولا يمثل افتراضًا لمسارات تثبيت WordPress الفعلية.

## السياق والمرحلة التالية

النشر المستهدف يبقى WordPress على xCloud Managed / NGINX / MySQL، بخادم 6 GB RAM و4 vCPU و100 GB تخزين. خط الإنتاج المحدد في المواصفات: WordPress 7.1.x، PHP 8.4.x وMySQL 8.4 LTS. تأكد على الموقع WordPress 7.1 وPHP-FPM 8.4 وNGINX 1.31.4 وMySQL 8.4.11. إعدادا الاستضافة المحدودان بمسارات MOL REST وأداة فحصهما موجودة في `scripts/deployment/`؛ طريقة الاستخدام في [دليل WordPress](docs/WORDPRESS_DEVELOPMENT.md#xcloud-rest-response-compatibility-2026-09-10).

تُراجع هذه الدفعة في فرع `codex/initial-renderer-editor`، ثم يستمر تنفيذ `DEVELOPMENT_PLAN.md` مع توثيق نتائج اختبار الأجهزة قبل بناء المحرر كاملًا. أضيفت إدارة المحتوى T-06 والواجهات العامة T-07 والقالب T-08 وأساس القارئ T-09 وهيكل المحرر T-10 وتحرير العناصر T-11 والحفظ والأقفال والمساهمات T-12/T-13/T-14. أضيفت إدارة الأنماط الشخصية والعمل والعامة T-15، مع الالتقاط وتثبيت حجم الملاءمة؛ أضيفت تحسينات الجوال T-16: تكبير بإصبعين دون تعديل العناصر، تحريك الصفحة، ولوحة خصائص 45%/85% تتبع مساحة لوحة المفاتيح. أضيف إرسال البلاغات ومراجعتها T-17، بما فيها المراجع والصلاحيات والحذف المتزامن. التالي التحصين T-18؛ قبول الأجهزة الفعلية في T-16 باقٍ. بوابة الأجهزة الفعلية مفتوحة، وكذلك التحصين النهائي وقبول الإطلاق. لا تعتبر اكتمال الجداول اكتمالًا لمرحلة T-04 كلها.

لا يحتوي المشروع أسرارًا أو بيانات دخول. تراخيص الخطوط الموزعة مرفقة في `THIRD_PARTY_NOTICES.md` و`licenses/`.
